apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: recipeapp
spec:
  selector:
    app: analytics-service
  ports:
    - port: 8001
      targetPort: 8001
  type: ClusterIP
