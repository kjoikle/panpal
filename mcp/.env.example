# URL for the PanPal recipe service (default: local development)
RECIPE_SERVICE_URL=http://localhost:8000

# URL for the PanPal scraper service (default: local development)
SCRAPER_SERVICE_URL=http://localhost:8002

# Internal service authentication key — must match INTERNAL_SERVICE_KEY in docker-compose.yml
INTERNAL_SERVICE_KEY=dev-internal-key
